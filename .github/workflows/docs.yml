# Docs are now served by the React frontend (frontend/src/routes/docs/).
# This workflow is intentionally disabled — remove the file to clean up.
name: Deploy docs (disabled)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docs are served by the SPA frontend. This workflow is a no-op."
